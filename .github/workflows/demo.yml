name: Deploy

on:
    push:
        branches: [dev]

jobs:
    test-application:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v2

            - uses: actions/setup-node@v1
              with:
                  node-version: 16
            
            - uses: docker-practice/actions-setup-docker@master 
            
            - uses: azure/setup-kubectl@v3
            
            - name: Run Add Test
              working-directory: ./
              run: |
                  npm install
                  pwd
                  ls
                  docker build . -t cordon-blue-front:latest
                  docker login -u 23snat -p asdfghjkasdfghjk 
                  docker image ls
                  docker push 23snat/cordon-blue-front:latest
